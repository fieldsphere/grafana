# CUR-71: feature flags dashboard

Source summary: `feature flags dashboard`

Implementation scope:

- Add a Labs navigation section next to Connections and Administration.
- Add a Feature flags dashboard under Labs.
- Show enabled feature flags from Grafana boot configuration.
- Allow local browser overrides for enabling, disabling, resetting, and clearing feature flag controls.
