# CUR-71: feature flags dashboard

Source field: `summary`

Specification: `feature flags dashboard`

Implementation scope:

- Add a Labs section in Grafana navigation near Connections and Administration.
- Add a Feature flags dashboard under Labs.
- Show feature flags enabled in the current Grafana boot config.
- Allow browser-local feature flag overrides from the dashboard.
- Mark the Labs navigation item as new so the menu shows a label next to the Labs icon.

