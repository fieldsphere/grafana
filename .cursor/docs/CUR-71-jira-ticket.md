# CUR-71 ticket record

Summary source of truth: feature flags dashboard

Jira write tooling was not available in this automation environment. The incoming Jira issue payload for CUR-71 was used as the source ticket context.

Implementation mapping:

- Adds a top-level Labs navigation item with a New badge.
- Adds `/labs` as the feature flags dashboard route.
- Lists enabled feature flags reported by Grafana boot config.
- Lets users set and reset browser-local feature flag overrides from the Labs page.
